## Packages
framer-motion | Page transitions, layout animations, and micro-interactions
date-fns | Formatting dates for listings and chat messages
clsx | Utility for conditional class names (usually installed but good to ensure)
tailwind-merge | Utility for merging tailwind classes (usually installed but good to ensure)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
